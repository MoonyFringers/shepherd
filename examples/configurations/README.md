# Examples for `shpd.yaml` configurations

- `minimal/`: smallest useful project skeleton
- `env-basic/`: one simple environment with one service
- `env-with-probes/`: environment-level and service-level readiness probes
- `svc-basic/`: reusable service template definitions
